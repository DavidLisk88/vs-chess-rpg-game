function express() {
  return {
    use: () => {},
    listen: (port, callback) => {
      console.log(`📡 Fake Express server listening on port ${port}`);
      callback();
    },
    get: () => {},
    post: () => {}
  };
}

module.exports = express;

