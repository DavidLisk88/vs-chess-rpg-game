module.exports = {
  json: () => {
    console.log('🧠 bodyParser.json() middleware loaded');
    return (req, res, next) => {
      // Simulate JSON parsing
      console.log('📨 Fake JSON body parsing');
      req.body = {}; // You can fake test data here if needed
      next && next();
    };
  }
};
