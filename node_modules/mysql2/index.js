// Basic simulated mysql2 for manual install
module.exports = {
  createConnection: function (config) {
    console.log('🔌 Pretending to connect to MySQL with:', config);
    return {
      connect: (callback) => {
        console.log('✅ Pretend MySQL connected');
        callback(null); // No error
      },
      query: (sql, values, callback) => {
        console.log('🧪 Simulated MySQL query:', sql, values);
        callback(null, { affectedRows: 1 });
      }
    };
  }
};
